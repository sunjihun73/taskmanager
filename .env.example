# .env.example 을 .env 로 복사한 뒤 실제 값을 입력하세요 (.env 는 git 커밋 금지)
# 운영 서버(외부 Tomcat)에서는 동일한 이름의 OS 환경변수로 설정

# Database
DB_PASSWORD=

# Azure Storage
AZURE_STORAGE_CONNECTION_STRING=

# Mailgun
MAILGUN_API_KEY=
